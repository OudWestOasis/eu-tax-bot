# Python
__pycache__/
*.pyc
.venv/
venv/

# Logs (runner-local, never needed in the repo)
*.log

# Local secret overrides — NEVER commit real tokens
config.local.json
.env

# Laptop-runner local files (machine-specific, not shared)
runner.lock
last_scan.txt

# OS junk
.DS_Store
Thumbs.db
